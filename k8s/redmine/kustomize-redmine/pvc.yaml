apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rm-app-pvc
  namespace: redmine
spec:
  storageClassName: gluster-heketi
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rm-app-files-pvc
  namespace: redmine
spec:
  storageClassName: gluster-heketi
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi